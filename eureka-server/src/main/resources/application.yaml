server.port: 8761

spring:

  application:
    name: EurekaServer

eureka:
  client:
    register-with-eureka: false # discovery service doesn't need to register with itself
    fetch-registry: false
